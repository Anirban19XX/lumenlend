{
  "name": "@lumenlend/shared",
  "version": "0.1.0",
  "description": "Shared TypeScript types, fixed-point math, and constants for LumenLend",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "rimraf": "^6.0.1"
  }
}
