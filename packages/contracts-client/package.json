{
  "name": "@lumenlend/contracts-client",
  "version": "0.1.0",
  "description": "Typed Soroban contract clients for LumenLend protocol",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@lumenlend/shared": "workspace:*",
    "@lumenlend/stellar": "workspace:*",
    "@stellar/stellar-sdk": "^13.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "rimraf": "^6.0.1"
  }
}
